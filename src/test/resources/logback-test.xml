<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Enable DEBUG for CaseAnalysisModeHandler to see authority selection logs -->
    <logger name="com.agent.service.handler.CaseAnalysisModeHandler" level="DEBUG" />
    
    <!-- Default root logger level -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
